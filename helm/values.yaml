# Default values for cd-demo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/pluralsh/plrl-cd-test
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "sha-783cc0c"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

# -- Prometheus metrics configuration
# The application exposes metrics on a separate port (9090) via prometheus_client
metrics:
  # -- Enable metrics port exposure in Deployment and Service
  enabled: true
  # -- Port number for the metrics endpoint
  port: 9090

# -- ServiceMonitor configuration for Prometheus Operator
# Requires metrics.enabled to be true
serviceMonitor:
  # -- Enable ServiceMonitor resource creation
  enabled: false
  # -- Scrape interval (e.g., "30s", "1m")
  interval: "30s"
  # -- Scrape timeout (must be less than interval)
  scrapeTimeout: "10s"
  # -- Path to scrape metrics from
  path: "/metrics"
  # -- Additional labels to add to the ServiceMonitor
  additionalLabels: {}
  # -- Namespace selector for the ServiceMonitor
  # If empty, only the release namespace is monitored
  namespaceSelector: {}
  # -- Relabeling configs for the endpoint
  relabelings: []
  # -- Metric relabeling configs for the endpoint
  metricRelabelings: []

ingress:
  enabled: true
  className: nginx
  annotations: 
    cert-manager.io/cluster-issuer: letsencrypt-prod
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: cd-demo.plrl.livingroom.cloud
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
   - secretName: cd-demo-tls
     hosts:
       - cd-demo.plrl.livingroom.cloud

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}
